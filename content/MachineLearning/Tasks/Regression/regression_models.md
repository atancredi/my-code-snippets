# Some experience with regression models


## Data normalization
Of course the label (y) data must not be normalized, or if it is necessary the prediction must be inverse transformed (e.g. if they weren't inverse transformed the score on the prediction would explode).

